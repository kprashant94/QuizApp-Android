<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#bfff00">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Exams"
        android:id="@+id/examsButton"
        android:textSize="25sp"
        android:onClick="onClickButtonExams"
        android:layout_marginBottom="20dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="#00ffff"
        android:textColor="#000000"
        android:layout_marginTop="150dp" />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Topics"
        android:id="@+id/topicsButton"
        android:textSize="25sp"
        android:onClick="onClickButtonTopics"
        android:layout_marginBottom="20dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="#00ffff"
        android:textColor="#000000"
        android:layout_below="@+id/examsButton" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Notes"
        android:id="@+id/notesButton"
        android:onClick="onClickButtonNotes"
        android:textSize="25sp"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="20dp"
        android:background="#00ffff"
        android:textColor="#000000"
        android:layout_below="@+id/topicsButton"
        android:layout_alignParentLeft="true" />


</RelativeLayout>
