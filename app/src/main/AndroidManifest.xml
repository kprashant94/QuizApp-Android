<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prashantkumar.questionhub" >

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ExamsListActivity"
            android:label="@string/title_activity_exams_list" >
        </activity>
        <activity
            android:name=".TopicsListActivity"
            android:label="@string/title_activity_topics_list" >
        </activity>
        <activity
            android:name=".QuestAnsActivity"
            android:label="@string/title_activity_quest_ans" >
        </activity>
        <activity
            android:name=".NotesActivity"
            android:label="@string/title_activity_notes" >
        </activity>
        <activity
            android:name=".QuizActivity"
            android:label="@string/title_activity_quiz" >
        </activity>
    </application>

</manifest>
